'''


sort by name then by number
iterate through name,
generate permutations of arr[namestart:nameend+1]
add dictionary and keep track of max
'''
from collections import defaultdict


a = ("~", "~")
b = ("zxdsf", "ASD", "ASdsa")
print(min(a, b))